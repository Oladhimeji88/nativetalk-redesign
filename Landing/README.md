
  # Build Exact Feature

  This is a code bundle for Build Exact Feature. The original project is available at https://www.figma.com/design/7c4tfoiYWbMEn2ook5dStL/Build-Exact-Feature.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  